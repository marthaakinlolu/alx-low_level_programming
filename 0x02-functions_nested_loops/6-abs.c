#include "main.h"

/**
 * _abs - print absolute value of an integer
 * @t: The integer to be computed
 * Return: the absolute value
 */
int _abs(int)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
