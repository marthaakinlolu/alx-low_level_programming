#include "main.h"

/**
 * print_array - prints n elements of an array of integers.
 * @a: input array
 * @n: input n elements
 * Return: 0
 */
void print_array(int *a, int n)
{
	int i;

	for (i = 0; i < n; i++)
		if (x != n - 1)
			printf("%d, ", a[x]);
		else
			printf("%d", a[x]);
	printf("\n");
}
